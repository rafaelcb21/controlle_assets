import 'package:flutter/material.dart';

class TecladoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => new Scaffold(
    appBar: new AppBar(
      backgroundColor: new Color(0xFFF44336),
    ),
    body: new Text('Tecladoooo'),
  );
}